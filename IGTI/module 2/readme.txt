This fold contains codes using several different classifiers
the notebook 'decision_tree_classifier' use this algorithm in order to predict if a pacient that asked for a consult will come to the consult or not.

The fold 'classifying wines' has notebooks with different algorithms to predict the quality wines
